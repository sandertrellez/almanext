"use client"
import { AuthProvider } from '@/context/auth';

export default function Chat() {
  
  return (
    <main className="mt-20 flex">
        <h1>Aqui va toda la información de clients</h1>
    </main>
  )
}
